export async function sendToGoogleVision(imageUri: string) {
    // Implemente aqui a chamada à API do Google Vision
    // Exemplo: converter para base64, enviar para endpoint, etc.
    // Retorne o resultado da comparação
}